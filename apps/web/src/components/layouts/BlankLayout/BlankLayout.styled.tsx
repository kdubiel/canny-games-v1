import { styled } from '@mui/material';

const Wrapper = styled('div')({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  minHeight: '100vh',
  overflow: 'hidden',
  paddingInline: 16,
});

export default {
  Wrapper,
};
